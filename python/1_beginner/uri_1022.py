n = int(input())
operador = None

def simplify(numer, denom):
    if numer >= denom:
        mult = numer
    else:
        mult = denom
    for i in range(2, mult):
        


def soma(expr):
    numer = int(expr[0]) * int(expr[6]) + int(expr[3]) * int(expr[2])
    denom = int(expr[0]) * int(expr[6])

    return

for i in range(0, n):
    l = input().split(" ")



